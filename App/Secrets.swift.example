import Foundation

/// Template. Copy to `Secrets.swift` (gitignored) and fill in real values.
enum Secrets {
    static let somfyUsername = "you@example.com"
    static let somfyPassword = "your-password"

    /// `device_url` from `./tahoma.py list`.
    static let deviceURL = "io://2310-2992-5397/600806"
    static let deviceLabel = "Roh velké"
}
